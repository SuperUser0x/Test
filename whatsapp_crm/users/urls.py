from django.urls import path

app_name = 'users'

urlpatterns = [
    # No app-specific URLs for 'users' at the moment.
    # Authentication URLs are handled by 'django.contrib.auth.urls'
    # included in the project-level urls.py under 'accounts/'.
]
